在 index.js 中做两处替换（建议用 Ctrl+H 查找替换）：

【第一处】删除偶数校验（整段删除下面 4 行，不要留空行）
查找：
      if (type === 'doubles' && firstSize % 2 !== 0) {
        return { success: false, message: '双打小队转时每队人数需为偶数' };
      }

替换为：（留空，即删除上述 4 行）

注意：删除后 "if (type === 'singles') {" 应紧接在 "if (tc !== 2) {...}" 的 } 后面。

【第二处】修改错误提示文案
查找：
生成对局失败（请确保每队人数一致，双打需为偶数）
替换为：
生成对局失败（请确保各队人数一致）

保存后重新上传并部署 generateTeams 云函数。
